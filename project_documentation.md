# توثيق شامل وخطة تطوير تطبيق "حافظ العهد" (Hafiz Al-Ahd)

## 1. التوثيق الحالي للتطبيق (Current Architecture & Features)
تم بناء التطبيق حالياً ليكون أداة أنيقة وعملية لعرض أوقات الصلاة والتاريخ بتصميم عصري ومتجاوب.

*   **الهدف الأساسي**: عرض أوقات الصلاة اليومية بناءً على الموقع الجغرافي للمستخدم، مع عرض التاريخين الهجري والميلادي.
*   **البنية التقنية (Tech Stack)**:
    *   **إطار العمل**: Flutter.
    *   **إدارة الحالة (State Management)**: استخدام نمط `Cubit` (Bloc) من خلال `PrayerTimesCubit` المربوط بـ `location_service`.
    *   **المكاتب الأساسية المُستخدمة**:
        *   `hijri_calendar`: للتعامل مع وضبط التقويم الهجري باللغة العربية.
        *   `wakelock_plus`: لإبقاء شاشة التطبيق مفعلة (مفيدة أثناء الانتظار للصلاة أو قراءة الأذكار لاحقاً).
        *   `shared_preferences`: للتخزين المحلي (مثل حفظ آخر موقع للمستخدم).
        *   `flutter_launcher_icons` و `flutter_native_splash`: لتخصيص أيقونات التطبيق وشاشة البداية.
*   **المميزات الحالية المنجزة**:
    *   **الواجهة المتجاوبة (Responsive Design)**: التطبيق مدعوم بالكامل للعمل على الهواتف الذكية (Mobile)، الأجهزة اللوحية (Tablet/Desktop)، وحتى الشاشات الصغيرة (Watch Layout).
    *   **تخصيص الواجهة**: أيقونات وشاشات بداية (Splash Screens) مخصصة تناسب الوضعين الداكن والفاتح، مع دعم مبدئي لتعدد اللغات (Localization).
    *   **الوقت والتاريخ**: قسم مخصص لعرض التوقيت بدقة والتاريخ (`TimeDateSection`).
    *   **مواقيت الصلاة**: شبكة متجاوبة لعرض الصلوات الخمس (`PrayerTimesGrid`).
    *   **تحديد الموقع المتقدم**: إمكانية تحديد الموقع يدوياً في حال فشل الـ GPS، مع حفظ الموقع محلياً (Caching) لسرعة الوصول بدون طلب إنترنت.
    *   **السمة العامة (Theme)**: تصميم فاخر ومريح للعين يعتمد على الألوان العميقة (`AppColors.deepBackground`).

---

## 2. الخطة المستقبلية لتطبيق إسلامي متكامل (Future Roadmap)
لتحويل "حافظ العهد" إلى تطبيق إسلامي شامل لا غنى عنه لأي مستخدم مسلم، نقترح التقسيم المرحلي التالي:

### المرحلة الأولى: ركائز التطبيق الأساسية (القرآن والأذكار)
*   **القرآن الكريم**:
    *   إضافة مصحف كامل (قراءة) بخط عثماني واضح.
    *   توفير خاصية الاستماع للتلاوات لأشهر القراء.
    *   حفظ آخر صفحة تم قراءتها (Bookmarks).
    *   توفير التفسير والترجمة للآيات.
*   **الأذكار والأدعية**:
    *   قسم مخصص لأذكار الصباح والمساء، أذكار النوم، وأذكار بعد الصلاة.
    *   مسبحة إلكترونية ذكية تحفظ عدد التسبيحات وتقدم المستخدم اليومي.

### المرحلة الثانية: الخدمات المكانية والتقنية (القبلة والإشعارات)
*   **تحديد القبلة**:
    *   إضافة بوصلة تفاعلية دقيقة تعتمد على حساسات الهاتف الجغرافي (Magnetometer) لتحديد اتجاه الكعبة.
*   **التنبيهات والأذان**:
    *   إشعارات محلية (Local Notifications) للتنبيه بوقت الصلاة، مع إمكانية تشغيل صوت الأذان كاملاً.
    *   تنبيهات قابلة للتخصيص (مثال: التنبيه قبل الصلاة بـ 15 دقيقة، أو تذكير بقيام الليل).

### المرحلة الثالثة: المحتوى التفاعلي والتخصيص
*   **التقويم الإسلامي والأحداث**:
    *   إضافة ويدجت أو صفحة توضح الأيام البيض، بداية الأشهر الهجرية، ومواسم الطاعات.
*   **الملف الشخصي والمتابعة (Habit Tracker)**:
    *   شاشة لتتبع العبادات (الصلاة في وقتها، قراءة الورد اليومي) مع نظام مكافآت (Gamification) لتشجيع المستخدم.
*   **الإعدادات المتقدمة**:
    *   تخصيص كامل للتطبيق (الوضع الداكن/الفاتح).
    *   دعم لغات متعددة لخدمة المسلمين في كل مكان.

---

## 3. قائمة المهام لتنفيذ الخطة (To-Do List)

### ✅ تم إنجازه (Done)
- [x] إعداد بنية التطبيق الأساسية (Architecture setup).
- [x] بناء تصميم الصفحة الرئيسية المتجاوب بناءً على حجم الشاشة.
- [x] ربط التطبيق بخدمات تحديد الموقع الجغرافي.
- [x] حساب وعرض مواقيت الصلاة والتاريخ الهجري والميلادي.
- [x] دعم التخزين المحلي (Caching) لحفظ الموقع وتوفير ميزة اختيار الموقع يدوياً.
- [x] تحديث أيقونات التطبيق وشاشات البداية (Splash Screens).
- [x] تجهيز بنية وتكوين تعدد اللغات (Localization).

### ⏳ المهام القادمة (حسب الأولوية لإطلاق نسخة تجريبية Beta)
- [ ] **نظام الإشعارات (MVP/Beta Blocker):**
    - [ ] تثبيت وإعداد مكتبة `flutter_local_notifications`.
    - [ ] جدولة الإشعارات بناءً على أوقات الصلاة التي يجلبها الـ Cubit.
- [ ] **البنية التحتية والبيانات المحلية:**
    - [ ] توسيع استخدام `SharedPreferences` أو إضافة `Hive` لحفظ إعدادات المستخدم، العلامات المرجعية للقرآن، وتقدم التسبيحات.
- [ ] **قسم الأذكار والمسبحة:**
    - [ ] إنشاء هيكل بيانات الأذكار وتصنيفاتها.
    - [ ] بناء شاشة تفاعلية للمسبحة تتيح الاهتزاز عند اللمس (Haptic Feedback) وتخزين العدد.
- [ ] **قسم القبلة:**
    - [ ] دمج مكتبة `flutter_compass` أو ما شابهها.
    - [ ] تصميم واجهة الرادار/البوصلة لضبط الاتجاهات.
- [ ] **قسم القرآن الكريم:**
    - [ ] إنشاء قاعدة بيانات (قراءة الـ JSON/PDF) لسور القرآن الكريم.
    - [ ] بناء واجهة عرض السور والأجزاء.
    - [ ] تصميم واجهة للقراءة المتجاوبة (Reading View).
    - [ ] إضافة مكتبة صوتيات (Audio Player) لتشغيل التلاوات.
