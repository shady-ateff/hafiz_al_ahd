# توثيق تطوير "المصحف الذكي" 📖

هذا الملف يوثق رحلة بناء وتطوير ميزة "المصحف الذكي" داخل تطبيق (حافظ العهد)، مع تسليط الضوء على التحديات الهندسية التي واجهتنا وكيف تم التغلب عليها للوصول إلى تطابق 100% مع المصحف المطبوع (مصحف المدينة المنورة - 15 سطر).

---

## 1. التحدي الأول: حجم التطبيق وأداء التحميل (Performance & Offline Support)
**المشكلة:**
القرآن الكريم يحتوي على 604 صفحة. استخدام صور (Images) لكل صفحة سيؤدي إلى زيادة حجم التطبيق بشكل كبير جداً (أكثر من 100 ميجابايت) وسيبطئ الأداء، بينما الاعتماد على الإنترنت سيمنع استخدام المصحف في وضع عدم الاتصال (Offline).

**الحل الهندسي:**
- **خطوط (QCF V2):** تم تبني خطوط "مجمع الملك فهد لطباعة المصحف الشريف" (QCF). هذه الخطوط ليست خطوطاً عادية، بل هي خطوط تعتمد على الـ Glyphs (رموز خاصة)، بحيث تمثل كل صفحة من الـ 604 ملف خط (Font) خاص بها.
- **التخزين المحلي (SQLite):** بدلاً من تخزين صور، قمنا بتخزين نص القرآن الكريم (نصوص عثمانية مخصصة للـ QCF) داخل قاعدة بيانات محلية (SQLite) سريعة جداً في الـ Queries، مما جعل حجم التطبيق صغيراً جداً وأداؤه فائق السرعة، ويعمل Offline بالكامل.

---

## 2. التحدي الثاني: إدارة تحميل الخطوط ديناميكياً (Dynamic Font Loading)
**المشكلة:**
Flutter لا يحمل 604 ملف خط في الذاكرة (RAM) تلقائياً، وإذا قمنا بإجبار التطبيق على تحميلها كلها سيتسبب ذلك في استهلاك ضخم للذاكرة (Memory Leak/OOM) وإغلاق التطبيق.

**الحل الهندسي:**
- **FontLoader API:** تم بناء نظام ذكي يقوم بتحميل ملف الخط الخاص بالصفحة المطلوبة **فقط** عند الحاجة إليها (Just-In-Time) باستخدام `FontLoader`، وتخزين حالة التحميل (Loaded) لتجنب تحميل الخط مرتين.
- **Pre-fetching:** قمنا ببرمجة الـ Cubit ليقوم بتحميل خط الصفحة الحالية والصفحة التي تليها (Next Page) والصفحة السابقة بشكل استباقي في الخلفية لضمان تنقل سلس (Smooth Scrolling) دون تقطيع.

---

## 3. التحدي الثالث: مشكلة تكرار قاعدة البيانات (Concurrency Bug)
**المشكلة:**
عند فتح المصحف لأول مرة، كان الـ `QCF_Seeder` يقرأ ملف النصوص ليملأ قاعدة بيانات SQLite. بسبب سرعة الـ Pre-fetching، كان التطبيق يطلب الصفحات 1 و 2 في نفس اللحظة، مما أدى إلى بدء عملية البناء (Seeding) مرتين متوازيتين (Race Condition). نتج عن ذلك تكرار الآيات مرتين في نفس الصفحة!

**الحل الهندسي:**
- **Future Lock (_initFuture):** تم وضع "قفل" (Lock) في مستودع البيانات يضمن أنه إذا كانت عملية الـ Seeding جارية، فإن أي طلبات أخرى تنتظر (await) نفس الـ Future بدلاً من بدء عملية جديدة، مما عالج التكرار جذرياً.

---

## 4. التحدي الرابع والأهم: محاذاة الأسطر (The 15-Line Exact Match)
**المشكلة:**
كان النص القرآني متوفراً لدينا كنص مقسم "بالآيات" (`mushaf-v2.txt`). عند تمرير النص لـ Flutter وتركه يقوم بلف النص تلقائياً (Word Wrapping) باستخدام `TextAlign.justify`، لاحظنا اختلافاً جوهرياً:
1. الصفحة تخرج أحياناً بـ 20 سطراً.
2. الكلمات التي ينتهي بها السطر لا تتطابق مع المصحف الورقي! 
*هذا الاختلاف مدمر لأي شخص يستخدم التطبيق لغرض الحفظ (التصوير البصري للصورة).*

السبب هو أن محرك نصوص Flutter لا يعرف عن "التعديلات اليدوية" والكشيدة التي أضافها خطاط المصحف (عثمان طه) لموازنة الـ 15 سطر.

**الحل الهندسي (النهائي):**
1. **تحديث البيانات:** تم التخلص من الملف المقسم بالآيات، وبناء سكربت بلغة Dart (`fetch_quran.dart`) لسحب داتا متخصصة من API رسمي لـ (Quran.com) تُعطينا النص القرآني **مُقسماً ومُصنفاً برقم السطر (Line by Line)** لكل صفحة من الـ 604.
2. **إعادة هيكلة قاعدة البيانات:** تم تخزين البيانات الجديدة في ملف `quran_lines.json`، وتحديث الـ Seeder ليحفظ كل (سطر) كعنصر مستقل في SQLite.
3. **تحديث واجهة العرض (UI):** بدلاً من رمي النص بالكامل في `Text` واحد، تم تحويل الصفحة إلى `Column` يحتوي على 15 نصاً مستقلاً (لكل سطر)، وتم تغليفهم جميعاً بـ `FittedBox` ليقوم بضبط مقاس الـ 15 سطر تلقائياً ليتناسب مع أي حجم شاشة هاتف دون أي تشوه (Responsive Alignment).

---

## النتيجة النهائية 🏆
تم إخراج مصحف ذكي:
- **سريع وخفيف:** لا يتجاوز بضعة ميجابايتات.
- **Offline 100%:** لا يحتاج لإنترنت بعد التثبيت.
- **متطابق بصرياً 100%:** مع طبعة مجمع الملك فهد (15 سطر)، نفس بدايات ونهايات الأسطر والصفحات.
- **صديق للحُفّاظ:** يدعم الذاكرة البصرية بدقة متناهية.
